# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

hello-world

# wifi-probe binaries
wifi-probe/wifi-probe
wifi-probe/wifi-probe-linux-amd64
wifi-probe/wifi-probe-linux-arm64

# dns-probe binaries
dns-probe/dns-probe
dns-probe/dns-probe-linux-amd64
dns-probe/dns-probe-linux-arm64

# jitter-probe binaries
jitter-probe/jitter-probe
jitter-probe/jitter-probe-linux-amd64
jitter-probe/jitter-probe-linux-arm64

# gateway-monitor binaries
gateway-monitor/gateway-monitor
gateway-monitor/gateway-monitor-linux-amd64
gateway-monitor/gateway-monitor-linux-arm64
