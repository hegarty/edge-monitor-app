replicaCount: 1

image:
  repository: pi-1.local:5000/wifi-probe
  pullPolicy: IfNotPresent
  tag: "local"

service:
  type: ClusterIP
  port: 9090
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "9090"
    prometheus.io/path: "/metrics"

ingress:
  enabled: true
  className: traefik
  host: wifi-probe.pi-1.local
  path: /metrics
  pathType: Prefix

resources: {}

podAnnotations: {}

metrics:
  enabled: true
  port: 9090

serviceMonitor:
  enabled: true
  path: /metrics
  interval: 30s
  scrapeTimeout: 10s
  labels:
    release: prometheus

env:
  PING_TARGETS: "1.1.1.1,8.8.8.8"
  HTTP_TARGETS: "https://ifconfig.me/ip"
  INTERVAL_SECONDS: "2"
