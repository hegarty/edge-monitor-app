replicaCount: 1

image:
  repository: pi-1.local:5000/dns-probe
  pullPolicy: IfNotPresent
  tag: "local"

service:
  type: ClusterIP
  port: 9091
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "9091"
    prometheus.io/path: "/metrics"

ingress:
  enabled: true
  className: traefik
  host: dns-probe.pi-1.local
  path: /metrics
  pathType: Prefix

resources: {}

podAnnotations: {}

metrics:
  enabled: true
  port: 9091

serviceMonitor:
  enabled: true
  path: /metrics
  interval: 30s
  scrapeTimeout: 10s
  labels:
    release: prometheus

env:
  DNS_TARGETS: "google.com,cloudflare.com"
  INTERVAL_SECONDS: "2"
